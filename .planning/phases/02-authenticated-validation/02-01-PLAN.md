---
wave: 1
autonomous: true
objective: Move secrets to WATCHDOG_* env vars with JSON fallbacks; document and gitignore local creds.
files_modified:
  - email_service.py
  - test_credentials.example.json
  - email_config.example.json
  - .gitignore
  - README.md
---

# Plan 02-01 — Secrets management

See `PHASE2-OVERVIEW.md` § Plan 1 for full spec.

## Task 1

Update `EmailService` to prefer `WATCHDOG_SMTP_*`, `WATCHDOG_EMAIL_*`, `WATCHDOG_SEND_ON` with backward-compatible fallbacks to legacy env names and JSON config.

## Task 2

Add `test_credentials.example.json`; ensure `test_credentials.json` documents env-first usage; update `email_config.example.json` and `.gitignore` (`*.env`, `.env`).

## Task 3

Document all `WATCHDOG_*` variables in `README.md` and verify `python3 -c "from email_service import EmailService; EmailService()"` runs without error.
