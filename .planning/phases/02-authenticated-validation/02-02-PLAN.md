---
wave: 2
autonomous: true
objective: Add mode and profile columns to runs table with idempotent migration and create_run API.
files_modified:
  - database.py
---

# Plan 02-02 — Database schema

See `PHASE2-OVERVIEW.md` § Plan 2 for full spec.

## Task 1

Extend `CREATE TABLE runs` with `mode` and `profile`; add guarded `ALTER TABLE` migrations for existing DBs.

## Task 2

Update `DatabaseManager.create_run()` to accept `mode` and `profile` and insert them; verify with sqlite smoke test.
