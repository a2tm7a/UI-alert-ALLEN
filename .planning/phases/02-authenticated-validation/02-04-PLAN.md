---
wave: 4
autonomous: true
objective: Wire authenticated runs into ScraperEngine after guest flow — one run per profile with shared context.
files_modified:
  - scraper.py
---

# Plan 02-04 — ScraperEngine integration

See `PHASE2-OVERVIEW.md` § Plan 4 for full spec.

## Task 1

Define `AUTH_PROFILES`, integrate `AuthSession`, and run sequential authenticated desktop+mobile passes per profile with fresh `PdpCache` and validation/email per profile.

## Task 2

Confirm guest path unchanged and authenticated path creates distinct `run_id` rows with correct `mode` / `profile`.
