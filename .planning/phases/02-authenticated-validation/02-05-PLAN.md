---
wave: 5
autonomous: true
objective: Report filenames and email subjects include guest vs authenticated profile context.
files_modified:
  - report_generator.py
  - email_service.py
---

# Plan 02-05 — Reporting updates

See `PHASE2-OVERVIEW.md` § Plan 5 for full spec.

## Task 1

Extend `ReportGenerator` with `mode` / `profile` parameters; filename suffix `_guest` vs `_auth_<PROFILE>`; header shows mode row.

## Task 2

Extend `EmailService.send_report()` with optional `profile`; subject includes `[Guest]` or `[Auth: PROFILE]`.
