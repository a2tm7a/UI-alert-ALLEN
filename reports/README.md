# Reports

This folder contains auto-generated verification run reports.

Each run of `scraper.py` produces a timestamped Markdown file here:

```
reports/report_YYYY-MM-DD_HH-MM-SS.md
```

> **Note:** Report files are excluded from version control (see `.gitignore`).  
> They are runtime output and will be generated fresh on every run.
