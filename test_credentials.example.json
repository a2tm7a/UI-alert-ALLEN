{
  "_comment": "Copy to test_credentials.json for local dev. In production, set WATCHDOG_TEST_FORM_ID and WATCHDOG_TEST_PASSWORD env vars instead.",
  "form_id": "YOUR_TEST_ACCOUNT_FORM_ID",
  "password": "YOUR_TEST_ACCOUNT_PASSWORD"
}
